# =============================================================================
# Repo root — optional reference only.
# Real configuration lives in:
#   backend/.env
#   frontend/.env
#   ai-service/.env
# =============================================================================

# Docker Compose (postgres/redis) — see docker-compose.yml
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=lingohub

# Typical local DATABASE_URL for backend/.env when using compose Postgres:
# postgresql://postgres:postgres@localhost:5432/lingohub?schema=public
